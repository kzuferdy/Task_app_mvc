import 'package:flutter/material.dart';

class SolutionFragment extends StatelessWidget {
  const SolutionFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Solution'),);
  }
}